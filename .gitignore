# Screenshots are evidence, not source. They are regenerated by `node verify.js`
# and at 2x device scale they run to well over a hundred megabytes, which does
# not belong in the history of a single-file game.
shots/

# there are deliberately no dependencies, so there is never a node_modules
node_modules/
